# pdf-generator-kit の使用方法

このフォルダは、既存の原稿プロジェクトに「高品質なPDF変換機能」を後付けするためのセットコードです。
以下の **1〜4 のステップ** を順に進めることで、執筆した原稿をプロ仕様のPDFとして出力できるようになります。

---

## 1. 導入（セットアップ）

まずは、自分のプロジェクトにこのキットを組み込みます。

1. **キットを配置する**
   原稿（.md ファイル）が置かれている一番上のフォルダ（プロジェクトルート）に、この `pdf-generator-kit` フォルダをコピーして配置します。
   
   ```text
   自分のプロジェクトルートフォルダ/
     ├── (自分の原稿ファイルなど)
     └── pdf-generator-kit/  <-- フォルダごと置く
   ```

2. **セットアップを実行する**
   配置したフォルダの中にある `setup.sh` を実行します。
   （※ ターミナルで `bash pdf-generator-kit/setup.sh` と打つか、環境によってはダブルクリックで実行可能です）

3. **完了を待つ**
   必要な設定や資材がプロジェクトルートに自動展開されます。既存のファイルを壊すことはありません。

---

## 2. まず動作を確認する（チュートリアル）

セットアップが正しくできたか、付属のサンプルを使って確認してみましょう。

1. **変換コマンドを実行する**
   ターミナルで以下のコマンドを入力して Enter を押してください。
   ```bash
   npm run pdf articles/SAMPLE.md
   ```
2. **PDFを確認する**
   新しく作られた `dist/pdf/` フォルダの中に、`SAMPLE.pdf` が出来ていれば成功です！

   ```text
   自分のプロジェクトルートフォルダ/
     └── dist/
         └── pdf/
             └── SAMPLE.pdf  <-- ここに出力される
   ```

---

## 3. 原稿を書く

執筆する原稿（.md ファイル）の置き場所には、大きく分けて2つのパターンがあります。

### パターンA：【推奨】標準の場所で書く
キットに最初から用意されている `articles/` フォルダの中に原稿を作成します。
*   **メリット:** すでにあるサンプル（SAMPLE.md）や画像フォルダ（images/）の構成をそのまま真似できるので迷いません。
*   **実行方法:** `npm run pdf articles/my-article.md`

```text
自分のプロジェクトルートフォルダ/
  └── articles/
       ├── SAMPLE.md
       ├── my-article.md　(自分の原稿)
       └── images/  <--- 最初から用意されています
            └── sample-image.png
            └── my-image.png　(自分の画像)

```

### パターンB：好きな場所（フォルダ）で書く
プロジェクト内の好きな場所にフォルダを作って管理することも可能です。
*   **ルール:** 画像を使う場合は、その原稿ファイルと同じ場所に `images/` フォルダを自分で作り、その中に画像を保存してください。
*   **実行方法:** `npm run pdf フォルダ名/my-article.md`

```text
自分のプロジェクトルートフォルダ/
  └── my-folder/ (自分で作ったフォルダ)
       ├── my-article.md　(自分の原稿)
       └── images/  <--- 自分で作成してください
            └── my-image.png　(自分の画像)
```

---

## 4. PDFを完成させる

原稿が書けたら、以下の鉄則を守ってPDFに変換します。

### コマンド実行の場所について
本キットは、プロジェクト内の**どのフォルダ（階層）から実行しても正しく動作します。** 階層を気にせず、そのままコマンドを入力して大丈夫です。

> [!TIP]
> **今いる場所（フォルダ）を確認するには？**
> 1. **ターミナルの表示を見る:** ターミナルの入力カーソルのすぐ左側に、現在のフォルダ名が表示されています。
> 2. **コマンドを打つ:** `pwd` と入力して Enter を押すと、現在の場所がフルパスで表示されます。

### 実行手順

> [!IMPORTANT]
> **パス指定のルール**
> コマンドをどこから実行する場合でも、原稿ファイルの指定は常に**「プロジェクトの一番上の階層（ルート）から見たパス」**で行います。

1. **コマンドを入力する**
   ターミナルで `npm run pdf` と打った後に（最後にスペースを1つ空けて）、プロジェクトルートから見た原稿ファイルまでのパスを続けて入力します。

   > [!TIP]
   > **パスを簡単に入力する方法（おすすめ）**
   > 左側のエクスプローラーから、PDFにしたい原稿ファイルを**右クリック**して、**「相対パスをコピー」** を選択します。その後、ターミナルに貼り付け（Cmd+V / Ctrl+V）すると、正しいパスが自動で入力されます。

   *   `npm run pdf articles/SAMPLE.md`
   *   `npm run pdf my-folder/my-article.md`

2. **PDFを受け取る**
   変換されたファイルは、常にルートにある `dist/pdf/` フォルダに出力されます。

---

## こだわり調整（カスタマイズ）

プロジェクトに合わせてデザインを変えたい場合は、**ルートにある** `styles/style.css` を編集します。
- `root` 部分の数値を書き換えるだけで、テーマカラーを一括変更できます。
- フォントの種類やサイズもこのファイルで自由に変更可能です。

---

## AIアシスタントへの指示

制作中にAIに相談する場合は、以下のように伝えてください。
> 「このプロジェクトの制作ルール（SKILL.md）を教えて」

キットが展開した `SKILL.md` をAIが読み込むことで、このキット特有のルールに基づいた的確なサポートを受けることができます。
